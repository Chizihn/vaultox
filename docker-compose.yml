services:
  postgres:
    image: postgres:16-alpine
    container_name: vaultox-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-vaultox}
      POSTGRES_USER: ${POSTGRES_USER:-vaultox}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-vaultox_dev_password}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-vaultox} -d ${POSTGRES_DB:-vaultox}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
